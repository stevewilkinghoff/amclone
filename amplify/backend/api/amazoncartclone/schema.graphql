type Todo @model {
  id: ID!
  name: String!
  description: String
}

type FMOHItem @model {
  id: ID!
  description: String!
  amount: String!
  reason: String!
}

type fmRoadmapData @model {
  id: ID!
  periodEnd: String!
  revenue: String!
  cogs: String!
  grossProfit: String
  overhead: String!
  noCustomers: String!
  noTransactions: String!
}

type customerCpmData @model {
  id: ID!
  periodEnd: String
  name: String
  revenue: String
  profit: String
  resonance: String
}
